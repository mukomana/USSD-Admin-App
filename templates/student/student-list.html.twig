{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link href="{{ asset('css/style.css') }}" rel="stylesheet" />
    <link href="{{ asset('css/content.css') }}" rel="stylesheet" />
{% endblock %}
{% block topbar %}
	{{ parent() }}
{% endblock %}
{% block sidebar %}
    <div class="sidebar">
      <a href="/">Home</a>
      <a class="active" href="/student_table">Student Results - Table</a>
      <a href="/piechart">Student Results - Chart</a>
      <a href="/manage_users">Manage users</a>
      <a href="/logout">Logout</a>
    </div>
{% endblock %}

{% block mainbody %}
	<div class="mainbody">
			<ul class="responsive-table">
				<li class="table-header">
					<div class="col col-1">MSISDN</div>
					<div class="col col-2">Name</div>
					<div class="col col-3">Surname</div>
					<div class="col col-4">Age</div>
					<div class="col col-5">Final Score</div>
				</thead>
			    {% for student in students %}
				<li class="table-row">
					<div class="col col-1"><a href="http://localhost:8000/viewstudent/{{ student.id }}">{{ student.msisdn }}</a></div>
					<div class="col col-2">{{ student.name }}</div>
					<div class="col col-3">{{ student.surname }}</div>
					<div class="col col-4">{{ student.age }}</div>
					<div class="col col-5">{{ student.finalScore }}</div>
				</li>	
		        {% endfor %}
			</ul>
	</div>
{% endblock mainbody %}
